import Metrics from "../(components)/Metrics";
import CompetitorCard from "../(components)/CompetitorCard";

function page() {
  return (
    <>
      <Metrics />
      <CompetitorCard />
    </>
  );
}

export default page;
